#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
	int T,n;
	cin>>T;
	while(T--)
	{
		cin>>n;
		int a[n];
		for(i=0;i<n;i++)
		cin>>a[i];
		sort(a,a+n);
		if(a[0]!=1)
		cout<<-1<<endl;
		for(i=1;i<n;i++)
		{
			for(j)
		}
	}
}
